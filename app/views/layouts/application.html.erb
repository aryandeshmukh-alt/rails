<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <header class="navbar">
      <h1>Blog App</h1>
      <nav>
        <%= link_to "Published", blogs_path %>
        <%= link_to "Drafts", drafts_blogs_path %>
        <%= link_to "New Blog", new_blog_path %>
      </nav>
    </header>

    <main class="container">
      <% if notice %>
        <p class="notice"><%= notice %></p>
      <% end %>

      <% if alert %>
        <p class="alert"><%= alert %></p>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
